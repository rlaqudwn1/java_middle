package codingtest_learn.KaKao;

/**
 * 1. banned_id.lenth 와 같은 id를 선별하고
 * 2. user_id 에서 banned_id *에 해당하는 좌표값을 *로 치환한다.
 * 3. uesr_id와 banned_id가 같을 경우 배열에 추가한다.
 * 4. 배열 길이가 1인 id의 값과 중복되는 값을 삭제한다.
 * 4-1 ex) while(true){
 *     if(id.equals(user.id)){
 *         while =true;
 *     }else while=false;
 * }
 */
public class BlackConsumer {
    public int solution(String[] user_id, String[] banned_id) {
        int answer = 0;
        return answer;
    }
    public static void main(String[] args){

    }
}
